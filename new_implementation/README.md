# New Implementation

This folder contains the clean re-implementation based on findings from `evaluation/`.

## Principles

- Follow the original intent and requirements, not the original code structure.
- Apply best practices: clean naming, separation of concerns, no dead code.
- Must be runnable and testable independently of `human_original/`.

## Structure

Organize files here as appropriate for the language/framework being used.
Document the chosen structure in a `STRUCTURE.md` file once implementation begins.
